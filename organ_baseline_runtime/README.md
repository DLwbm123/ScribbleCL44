# Completed Organ baseline source snapshot

Exact deployed Python source closure for organ_baseline_tuning_20260911, including the dynamic main.py/runner.py entry. Source dependencies were captured after completion; the running campaign did not modify its source. Use run.py in a source directory under a campaign root, with the existing Organ half-data inputs and interpreter. Tests are in test_baseline_tuning.py. Training and selection details are in ../reports/organ_baseline_tuning_completion_20260911.md.

This snapshot preserves the completed 12-screen + 3-formal campaign independently of later edits to the root runner. Large artifacts, private inputs, and raw logs remain on the experiment server.
